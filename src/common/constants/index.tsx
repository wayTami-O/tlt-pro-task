export type linksType = {
    index: string,
    business: string,
    ytp: string,
    portfolio: string,
    team: string,
    process: string,
    clients: string,
    order: string
}

export const links: linksType = {
    index: '/',
    business: '/business',
    ytp: '/ytp',
    portfolio: '/portfolio',
    team: '/team',
    process: '/process',
    clients: '/clients',
    order: '/order'
}